jQuery(function($) {
	"use strict";
	$.validate();
	//alert();
	/*
	$(".icon").click(function(){
		var lang = $(this).attr("href");
		var parent = $(window.parent.location).attr('href');
		if (lang == "/es"){
			alert(parent + lang);
		}
		if (lang == "/pt"){
			alert(parent + lang);
		}
		if (lang == "/"){
			alert(parent + lang);
		}
	});
	*/ 
});
